const styles = (theme) => {
  return {
    root: {
      flexGrow: 1,
      background: '#e6f2ff',
      maxWidth: '500px',
      borderRadius: '10px',
    },
    box: {
      margin: 20,
    },
    error: {
      color: '#ff0000',
    },
    header: {
      'font-family': 'Open Sans Condensed',
      'font-weight': 'bold',
    },
  };
};

export default styles;
